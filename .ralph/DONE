Review delivery moved into workers: durable review_deliveries claimed under per-repo repo_leases, ref_writes consulted by the integrity check, stale claims reaped.
- Web processes only enqueue a `review_deliveries` row on approve / retry-merge; workers claim it under a `repo_leases` row for the repo, run the unchanged merge / PR path from `reviewService.ts`, and record moved refs in `ref_writes`.
- The run-end integrity check consults `ref_writes` so a loop overlapping a sibling merge does not report tampering; leases and deliveries owned by a dead worker are reaped with the stale window.
- Final task: extended `src/server/splitProcesses.test.ts` with a four-process end-to-end test that fires two approvals from two web processes at once, asserts both merges land serially (disjoint `claimed_at`/`ended_at`, `repo_leases` empty, ≥2 `ref_writes` for main), and that a concurrently looping card settles in review with no `integrity violation` in its `run.finished` events.
- Key files: `drizzle/0020_wonderful_martin_li.sql` + `src/db/schema.ts` (tables), `src/server/reviewService.ts` (enqueue / claim / deliver), `src/server/repoLeases.ts` (per-repo lease), `src/server/integrity.ts` (`ref_writes` recording and consultation), `src/server/orchestrator.ts` (worker pump + reaper), `src/server/splitProcesses.test.ts`.
- Verified with `npx tsc --noEmit && npx vitest run src/server/splitProcesses.test.ts` (skipped without `RADULF_SPLIT_CHECK=1`; run `make check-split` for the live four-process check).
