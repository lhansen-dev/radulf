Split-process review-delivery gate passes; remaining failing check is an inverted grep for a stale comment that was correctly removed
- Task 1: `splitProcesses.test.ts` ENOENT fixed (recreate `mock-output/` after `git rm` empties it) — commit eb5aab0.
- Task 2: `checkRepoIntegrity` (`src/server/integrity.ts`) waits on the per-repo lease (`waitForRepoLeaseRelease`, 30s settle) and re-reads `ref_writes` against the same ref snapshot before judging an unexplained ref move — closes the sibling-worker `git commit` → `recordRefWrite` race; unit test added in `integrity.test.ts` — commit b306ceb.
- Task 3: `claimDelivery` (`src/server/reviewService.ts`) terminates a pending delivery whose repo record vanished (finish ok=0, card → needs_attention, review.decided event) instead of leaving it pending forever; the stale "in-process merge claim" orphan-sweep comment in `orchestrator.ts` was rewritten — commit 2528a2e.
- Task 4: `next build --webpack` + `RADULF_SPLIT_CHECK=1 npx vitest run src/server/splitProcesses.test.ts` green twice (9/9) — commit 34e8e95.
- Task 5: `grep -c 'in-process merge claim' src/server/orchestrator.ts` returns 0 by design — the plan's finding asked for that phrase to be removed because the in-process claim no longer exists (spec 25 decision 6). The check is inverted and should be `! grep -q …`; no code was changed to game it.
