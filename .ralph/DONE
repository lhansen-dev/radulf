Spec 29 base sync waits for the per-repo delivery lease to be free instead of acquiring it; spec text corrected; beta merge conflicts resolved.
- `src/server/orchestrator.ts`: the pre-evaluation base sync calls `waitForRepoLeaseRelease(repo.path, LEASE_SETTLE_MS)` (imported from `./integrity`) so it waits for a delivery in flight to finish rather than taking the lease itself.
- `specs/29-sync-and-gate-before-evaluation.md`: two sentences corrected to say the sync waits for the lease to be free and never holds it.
- Merge with `beta`: `docs/DESIGN_HISTORY.md` keeps both the spec 28 and spec 29 rows (28 before 29); `src/server/mockPipeline.test.ts` keeps both the `base-conflict` and `graph epic` pipeline tests.
- Verification: `git diff --check` exits 0 with no leftover conflict markers.
