Plan critic stage (spec 30) finished: reviewer findings fixed
- Removed the "Numbered 30 because 28…" paragraph from specs/30-plan-critic.md (grep -c '28' now prints 0; its exit 1 is the expected success outcome).
- Added critic fields to the request-validation test's expected object.
- Mapped `critique` runs to `critique.jsonl` in the runs API route (src/app/api/runs/[id]) with a route test.
- Named the plan critic in stage diagnoses.
- Resolved beta merge conflicts (docs/DESIGN_HISTORY.md, src/server/mockPipeline.test.ts); final iteration confirmed no conflict markers remain in tracked sources and the tree is clean.
