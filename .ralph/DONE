Plan critic stage (spec 30) reviewer findings resolved; final check misreports success
- Removed the "Numbered 30 because 28…" paragraph from specs/30-plan-critic.md (`grep -c '28'` → 0; exit 1 is grep's zero-count status, not a failure)
- Added critic fields to the request-validation test's expected object
- Mapped `critique` runs to `critique.jsonl` in the runs API route with a route test
- Named the plan critic in stage diagnoses
- Key files: specs/30-plan-critic.md, src/app/api/runs/[id]/*, stage diagnosis module, request-validation test
