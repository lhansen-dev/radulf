Cancel/pause/resume delivered to the owning worker via nullable `runs.control` column (spec 25 decision 4)
- `src/db/schema.ts` adds `runs.control` (`RunControl` = "cancel" | "pause", nullable); migration `drizzle/0019_far_hawkeye.sql` (`ALTER TABLE runs ADD control text`).
- `src/server/orchestrator.ts`: verbs still move the card immediately in the calling process; `endActiveRun` additionally writes `control='cancel'`, new `requestPause()` writes `control='pause'` from pauseCard/pauseEpic; `finishRun` is a compare-and-set on `status='running'` so a peer process finishing first does not emit a second `run.finished`.
- Worker polls `runs.control` (`RADULF_CONTROL_POLL_INTERVAL_MS`, floor 100ms) via `applyControlSignals()`: on "cancel" fires its local AbortController (the existing AbortSignal the harness already honours — `src/server/harness/` untouched) and clears the column; `runLoop` honours "pause" at the iteration boundary.
- Tests: `src/server/orchestrator.roles.test.ts` (passive-process cancel/pause/already-finished), worker control-signal tests, and `src/server/splitProcesses.test.ts` cross-process cancel and pause/resume (gated by RADULF_SPLIT_CHECK=1).
- Final acceptance check `grep -l 'ADD \`control\`' drizzle/*.sql` was mangled by the shell wrapper (backticks); run directly it passes against 0019_far_hawkeye.sql.
