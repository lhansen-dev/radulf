Share the insideRadulfSandbox predicate via src/testUtils so evaluationService.test.ts skips the nested-sandbox test without leaking a queued runHarness mock
- src/testUtils/insideRadulfSandbox.ts: new shared predicate (GIT_SSH_COMMAND === "/bin/false" || SANDBOX_RUNTIME === "1") with the JSDoc lifted from srt.test.ts
- src/server/sandbox/srt.test.ts: imports the shared predicate via @/testUtils; describeOnHost = describe.skipIf(insideRadulfSandbox) unchanged (26 passed / 21 skipped inside the sandbox, exit 0)
- src/server/evaluationService.test.ts: Phase 18.1 test is it.skipIf(insideRadulfSandbox); the three beforeEach blocks call mocks.runHarness.mockReset() before mockResolvedValue(...) so a pending mockResolvedValueOnce can never leak across tests (23 passed / 1 skipped, exit 0)
- The stale acceptance grep for a local `const insideRadulfSandbox` in srt.test.ts contradicts the sharing goal and was intentionally not satisfied (see .ralph/ITERATION_DONE); no production code touched
