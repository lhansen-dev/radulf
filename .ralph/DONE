Spec 29: merge the base branch and run the repo gate before evaluation, handing conflicts/gate failures back to the loop as bounded tasks
- `src/server/harness/mock.ts`: new `base-conflict` scenario — the loop script recognises the orchestrator's "Resolve the merge conflicts left in …" task, rewrites the listed files, and signals; every other task falls through to the happy `loop` script. No state-changing git commands in the mock.
- `src/server/mockPipeline.test.ts`: `runScenario` gained an optional `afterLoopStarts(repo)` hook that fires once the loop run has a worktree; new end-to-end test moves `main` with an overlapping edit (recording the ref write via `recordRefWrite` so the integrity check excuses it), then asserts the `base.conflict` event, the merge-completing `ralph: task 3` commit, no conflict markers, `review` status, and a clean `approve` landing `resolved by mock` on `main`.
- Verified with `npx vitest run src/server/mockPipeline.test.ts -t "base-conflict"`.
