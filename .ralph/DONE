Events and live transcripts fan out to every Radulf web process (spec 25, decision 5) — split check verified, mechanism documented.
- `make build && make check-split` passes: boot, cross-process card.created fan-out, drive-to-review with live transcript seen from a second web process, SIGTERM drain (src/server/splitProcesses.test.ts; fan-out intervals lowered to their floors for mock-provider pacing).
- Mechanism: every process runs a polling events tailer (src/server/eventsTail.ts) that replays rows other processes inserted into the `events` table onto the local bus, deduping locally-emitted ids; the web role additionally runs a per-running-run transcript watcher registry (src/server/transcriptWatchers.ts) that owns exactly one `startTranscriptPush` per run and pushes chunks over SSE. Wired in src/server/boot.ts.
- Stage (src/server/stage.ts `runWithTranscript`) deliberately does not call `startTranscriptPush`; its doc comment now says so by name and points at the registry (this iteration's only code change — the stale grep check assumed the old in-stage push).
- Docs: docs/ARCHITECTURE.md (Events and the UI + Boot), README.md `make check-split` row, spec 25 decision 5 implementation amendment.
