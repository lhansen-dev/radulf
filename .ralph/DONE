Split-process review-delivery gate passes and is stable (two consecutive green runs)
- Production bundle built with `NODE_ENV=production node_modules/.bin/next build --webpack` (exit 0); the test rebuilds `dist/worker.mjs` itself via `make build-worker`.
- `RADULF_SPLIT_CHECK=1 npx vitest run src/server/splitProcesses.test.ts` run twice back-to-back: both exit 0, 9/9 tests pass, including "two approvals from two web processes deliver serially and a loop overlapping a sibling merge reports no tampering" — the lease wait in `checkRepoIntegrity` (`src/server/integrity.ts`, polling `leaseHolder` from `./repoLeases` on `ref moved:`/`ref appeared:`) takes effect and the overlapping card never lands in `needs_attention`.
- `src/server/splitProcesses.test.ts`: the overlapping-merge test now polls tightly for any post-start status (`looping`/`evaluating`/`review`) rather than waiting for exactly `looping`; on a fast machine the loop finishes in ~100 ms and waiting for `looping` alone timed out on a card already in review. Fails fast with worker log tails if the card hits `needs_attention`.
- No production code changed this iteration; `src/server/integrity.ts` and `src/server/repoLeases.ts` were verified as already correct.
