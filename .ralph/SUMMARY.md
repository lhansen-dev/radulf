Adds a read-only plan critic stage (spec 30) between planning and the loop: a new `critique` run kind with its own provider/model/reasoning/timeout settings and prompt template reads the card, scoping thread, named specs and the three plan artifacts, then writes `.ralph/CRITIQUE.md` in the evaluator's verdict format. Approve moves the card to Ready or plan review as planning does today; revise re-plans with the feedback, and a third consecutive revise escalates to plan review. It is on by default for breakdown pieces (`planCriticMode`) with a per-card tri-state override, records `critique.decided` events, enforces the verdict-only write rule, and is covered by service tests plus mock-pipeline approve/revise scenarios.

Evaluator verdict: revise — the spec still cites the number 28 (criterion requires none), `src/server/requestValidation.test.ts` fails on HEAD because its expected create-request object lacks the new `criticModel` field, and the runs API's transcript map omits `critique`, so critic transcripts cannot be opened from the UI.
