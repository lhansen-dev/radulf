Spec 29: when a loop signals DONE, the orchestrator now merges the base branch into the worktree (a merge commit on the run branch; a conflict becomes a resolve-conflicts task the loop finishes and the next bookkeeping commit completes) and then runs the repository gate, handing a non-zero exit back to the loop as a repair task quoting the output tail. Both are bounded to two rounds per run, after which the run fails with the reason on the card; the evaluator reuses the `.ralph/GATE.md` the loop's DONE path wrote. Adds `src/server/baseSync.ts`, `gateRepairTaskText`, the `base-conflict` mock scenario, lifecycle and end-to-end tests, and the spec 29 record. Sent back for one fix: the sync currently takes the per-repo delivery lease (`acquireRepoLease`) rather than only waiting for it to be free, which fails two acceptance criteria and can fail a run when a delivery outlasts the 30 s settle window.
