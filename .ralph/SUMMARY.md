When a loop signals DONE, the orchestrator now waits for the repo's delivery lease to be free, merges the base branch into the worktree (a clean merge becomes a merge commit on the run branch; a conflict becomes a resolve-conflicts task the loop's next bookkeeping commit completes), then runs the repository gate before evaluation, handing a failing gate back to the loop as a repair task quoting the output tail. Both are bounded to two rounds per run, the evaluator reuses the loop-written `.ralph/GATE.md`, and spec 29 records the decision; this follow-up changed the sync to wait for the lease rather than acquire it and corrected the spec text to match. Note for approval: `beta` moved under this branch, so the merge will conflict trivially in `docs/DESIGN_HISTORY.md` and `src/server/mockPipeline.test.ts` (both sides appended).
