Makes the test suite runnable inside Radulf's own sandbox: the four test files that wrote to a literal `/tmp` (`bookkeeping`, `git`, `harness/index`, `harness/streamLiveness`) now use `os.tmpdir()`-based scratch dirs with cleanup; the three real-runtime suites in `srt.test.ts` skip themselves when nested (signal: `SANDBOX_RUNTIME=1` or `GIT_SSH_COMMAND=/bin/false`, documented in the test); and `runSandboxedCommand`/`wrapUnderPolicy`/`createSandboxedBashOperations` accept the run's `tmpdir` and publish it as `CLAUDE_CODE_TMPDIR` for the duration of the serialized wrap, so srt's injected `TMPDIR` is the run-private directory rather than the non-existent `/tmp/claude` (plumbed at gate, installGate, acceptanceProbe and pi; documented in `docs/SANDBOXING.md`).

Evaluator verdict: revise. All targeted criteria pass and the change is sound, but `make test` inside the sandbox still fails 3 tests in `evaluationService.test.ts` — a pre-existing, in-scope nested-sandbox issue (a `sandboxEnabled=true` test leaks a `mockImplementationOnce` when preflight fails nested). One small test-only fix remains.
