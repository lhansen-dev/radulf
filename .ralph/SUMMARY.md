Spec 29: when a loop writes DONE, the orchestrator now waits for the repo's delivery lease to be free (never taking it), merges the base branch into the worktree (`src/server/baseSync.ts`; a clean merge becomes a merge commit, a conflict becomes a resolve-conflicts task the loop's next bookkeeping commit completes), then runs the repository gate in the worktree and hands a failing gate back to the loop as a repair task quoting the output tail. Evaluation starts only once the branch is synced and the gate passes; sync conflicts and gate failures together get at most two rounds before the run fails with the reason on the card. The evaluator reuses the `.ralph/GATE.md` the loop left, `src/server/harness/` gains only a `base-conflict` mock scenario, and the approval path in `reviewService.ts` is unchanged.
