Adds a read-only plan critic stage (spec 30) between planning and the loop: after the planner finishes, a `critique` run with its own provider/model/reasoning settings reads the card, scoping thread, named specs and the three plan artifacts, and writes `.ralph/CRITIQUE.md` in the evaluator's verdict format. Approve moves the card to Ready or plan review as before; revise re-plans with the critic's feedback in the planner prompt, escalating to human plan review on the third consecutive revise. On by default for breakdown pieces (`planCriticMode`), with a per-card `planCritic` override, full telemetry/transcript, a `critique.decided` activity event, UI controls, mock-provider scripts, and rejection of any file change other than the verdict. This revision removed the spec's numbering paragraph, fixed the `requestValidation.test.ts` regression, made critic transcripts reachable via `GET /api/runs/[id]`, and named the critic in stage diagnoses.
