Adds a nullable `runs.control` column (`"cancel" | "pause"`, migration 0019) so cancel/reset/pause issued from a web-only process reach the worker that owns the run: the verbs still move the card and finish the run row immediately in the calling process, `finishRun` is now a compare-and-set on `status='running'` so the first terminal cause wins across processes, and each worker polls `runs.control` every `RADULF_CONTROL_POLL_INTERVAL_MS` for the runs whose controller it holds, firing the existing `AbortSignal` on `cancel` and honouring `pause` at the iteration boundary. The harness is untouched; spec 25 decision 4 is amended.

Verdict this round: revise. All unit/static criteria pass (roles, lifecycle, stage suites, tsc, eslint, migration, spec), but the whole-card gate `make check-split` fails deterministically: the new cross-process cancel test cancels before the worker has registered its controller, so the poll never sees the run and `control` is left as `'cancel'`. Fix verified in a scratch copy: wait for the run's first `iterations` row before cancelling (and, recommended, clear a pending `cancel` in `runLoop`'s `finally`).
