Puts the last three single-process assumptions under database coordination so several worker processes can share one SQLite file: improvement-run drivers now claim a per-run lease (`worker_id`/`heartbeat_at` on `improvement_runs`, migration 0021) with heartbeat, release and stale takeover; the schedule tick fires only after a compare-and-set on `lastFiredAt` (`claimScheduleFire`); the hourly retention timer prunes only when it wins a per-UTC-day marker (`claimDailySweep`). `compose.yaml` is split into a `web` service (`RADULF_ROLES=web`, `/api/health`, default Docker profiles, all caps dropped) and a `worker` service (`node dist/worker.mjs`, heartbeat-row health check via a new `radulf-worker-health` script, the only service carrying the seccomp/AppArmor/systempaths relaxations); the Makefile gains `web` and `check-compose` targets; `docs/ARCHITECTURE.md`, `docs/DOCKER.md` and `docs/SANDBOXING.md` are rewritten for the two-role topology. All sandbox-verifiable criteria pass; the Docker-level criteria (compose up, mock-provider pipeline, `--scale worker=2`) need a Docker host to confirm.
