Makes the repository's own test suite pass inside Radulf's sandbox with `TMPDIR` left as the sandbox sets it. Tests that wrote to a literal `/tmp` now use `os.tmpdir()`; the sandboxed `TMPDIR` becomes the run-private directory via `CLAUDE_CODE_TMPDIR` in `runSandboxedCommand`; and a single shared predicate, `src/testUtils/insideRadulfSandbox.ts` (`SANDBOX_RUNTIME === "1"` or `GIT_SSH_COMMAND === "/bin/false"`), skips the tests that need to start a nested sandbox — the srt preflight/real-runtime rows and the sandbox-on Phase 18.1 evaluator test — with `evaluationService.test.ts` also `mockReset()`ing `runHarness` so a queued mock can never leak across tests. Inside the sandbox `make test` now exits 0 (1296 passed, 31 skipped: 9 split-process + 22 nested-sandbox); on a plain host nothing is skipped and the suite runs as before.
